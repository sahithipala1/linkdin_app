m = ["()", "{}", "[]"]

# Initial assumption
found_duplicate = False

# Processing
for i in range(len(m)):
    for inner_index in range( len(m):
        if  m[i] == m[inner_index]
            found_duplicate = True

# Final answer
print(found_duplicate)
